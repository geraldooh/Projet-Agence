<form method="post" action="{{ path('biens_delete', {'id': bien.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce bien ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bien.id) }}">
    <button class="btn btn-danger mt-1">Delete</button>
</form>
