{% extends 'base.html.twig' %}

{% block title %}Edit Biens{% endblock %}

{% block body %}
    <h1>Editer le Bien</h1>

    {{ include('biens/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary mt-1" href="{{ path('biens_index') }}">back to list</a>

    {{ include('biens/_delete_form.html.twig') }}
{% endblock %}

